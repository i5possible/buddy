import React from 'react';
import {style} from './index.scss';

const Content = ({}) => {

    return (
        <div className={style}>
            
        </div>
    );
};

Content.propTypes = {

};

export default Content;
